name: Deploy OKX Bot
on: [push]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - name: Deploy using the master script
      run: |
        # نادي "السكرتير" واديه "المسار الكامل" واسم pm2
        sudo /usr/local/bin/deploy_bot.sh /root/bots/okx okx-bot
